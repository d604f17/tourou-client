import React, {Component} from 'react';
import Radium from 'radium';

const TimesInput = ({}) => {
  return (
      <div>
        asd
      </div>
  );
};

const styles = {

};

export default Radium(TimesInput);